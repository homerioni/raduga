export const apiRoutes = {
  products: '/products',
  product: '/product',
  categories: '/categories',
  category: '/category',
} as const;

export const routes = {
  main: '/',
  catalog: '/catalog',
  product: '/product',
} as const;
